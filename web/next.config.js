/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'lh3.googleusercontent.com' , 'img.freepik.com' , 'images.pexels.com','example.com','http://localhost:4000'],
      },
}

module.exports = nextConfig
